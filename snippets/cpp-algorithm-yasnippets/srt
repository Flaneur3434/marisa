# -*- mode: snippet -*-
# name: sort(RndIt beg, RndIt end[, Cmp cmp]) -> void
# key: srt
# --
std::sort(${1:std::begin(${2:container})}, ${3:std::end($2)}${4:, ${5:[](auto ${6:const& }lhs, auto $6rhs) { ${ return lhs < rhs;} }}});$0